package com.example.helpmav;

import android.os.Build;

import java.time.LocalTime;
import java.time.format.DateTimeFormatter;

public class TimeUtil {

    // Convert epoch seconds to millis
    public static long convertEpochSecondsToMillis(long epochSeconds) {
        return epochSeconds * 1000L;
    }

    // Convert seconds from midnight to LocalTime



}
